package org.greatgamesonly.core.universalcalculator;

import org.junit.jupiter.api.Test;

@SpringBootTestWrapper
class SpringWorkAssessmentApplicationTests {

	/*
	TODO lacked time to complete unit tests, this is very important, but also a few good tests to test the features more so than the implementation
	 */

	@Test
	void contextLoad() {
		// the context will be loaded which is always done when a spring app starts up, any breaking errors happening there will likely cause this test to fail
	}

}
